<% if @categories.any? %>
<h1>Categories</h1>
  <ul>
    <% @categories.each do |category| %>
      <li><a href="/categories/<%= category.id %>"><%= category.group %></a></li>
    <% end %>
  </ul>
<% else %>
  <p>There are no categories</p>
<% end %>
